rootProject.name = 'airtable'

/**
 * The core Java API interface
 */
include 'api'

/**
 * Cache for read only.
 */
include 'cache'

/**
 * Mirror for copying data from your own database to Airtable.
 * Using Airtable as a stateless data view for EDA.
 */
include 'mirror'
